a = [1, 2, 3]
b = a
a.append(4)

#print(b)

# 经典说法：
# 不要把变量比喻为盒子——便利贴才是变量的真正用途。
# b = a语句不是把a盒子中的内容复制到b盒子中，而是在标注为a的对象上再贴一个标注b。


# Stein教授讲到复制方式：

class Gizmo: # Gizmo类是一个空类，没有属性和方法
    def __init__(self):
        print(f'Gizmo id: {id(self)}') # 打印对象id, id函数返回对象的唯一标识符,
                        # 唯一标识符是对象在内存中的地址，因此，id函数返回的是对象的内存地址。

x = Gizmo() # 创建对象
print('\n')
y = Gizmo() # 创建对象
print('\n')
print(dir()) # 查看当前作用域中的变量

# ChatGPT 讲解：

'''

id() 的语义：

在 CPython，id(obj) 实际上是对象在内存中的地址（所以通常用 hex(id(obj)) 可以看到像 0x7f... 的十六进制地址）。

id() 保证在对象“存在期间”唯一，相同的整数可能在对象销毁后被复用给新对象。

对象创建与 __init__：

创建对象实际分两步：先分配内存并创建对象（调用 __new__），再由 __init__ 初始化。通常你只需要理解 __init__ 在实例化时被调用用于初始化。

dir() 返回了什么：

dir()（无参数）返回当前本地作用域或模块的变量名（字符串列表）。在脚本的模块级别，本地就是全局，所以它基本等同于 globals().keys() 的排序列表。

如果你运行在 REPL（如交互式解释器），dir() 会把当前交互会话的名字列出来。

空类 Gizmo 没有属性：

这个类没有声明属性或方法（除了 __init__），所以实例的 __dict__ 通常是空的：

python
复制
编辑
print(x.__dict__)  # {}
对象比较：

x is y 会是 False（它们是不同实例）。

id(x) == id(y) 也通常是 False，因为 id 不同。

关于 print('\n')：

如果你发现输出有多余的空行，改成 print()（只产生一空行）会更常用。

'''